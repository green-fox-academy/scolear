#ifndef OPENCV_PRACTICE_SATURATION_H
#define OPENCV_PRACTICE_SATURATION_H

#include <opencv2/opencv.hpp>

cv::Mat create_saturated(const cv::Mat& original, double alpha, int beta);

#endif //OPENCV_PRACTICE_SATURATION_H
